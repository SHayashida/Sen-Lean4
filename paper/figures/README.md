# Figures Directory

- Place generated paper figures under `paper/figures/generated/`.
- Do not commit large transient figure batches unless required for camera-ready packaging.
- Regenerate using repository scripts (for example: `scripts/plot_frontier.py`, `scripts/plot_eval.py`).
