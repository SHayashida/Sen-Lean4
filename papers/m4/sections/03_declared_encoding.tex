\section{Declared Encoding And Scope}

This section will define the role of the declared M4 encoding as the object of
study. The finite audit is conditional on that declared encoding. The paper may
use the encoding to organize the Sen24 repair and reporting structure, but it
must not silently treat encoding correctness as already proved.

TODO: Introduce the declared Sen24 scope, the residual mask organization, the
fixed right-condition vocabulary, and the obstruction-shape coordinate only
after the reader-facing motivation has been established.

TODO: State that Level C remains future work. This includes a representation
map between Lean profiles and finite schema profiles, a mapping from Lean
strict preference predicates to generator variables, SAT assignment semantics,
right-clause correctness, and generated-CNF artifact correctness.

Safe claim-boundary note: claims in this section should use wording such as
``under the declared M4 encoding'' and ``within the declared Sen24 scope.'' Do
not write that semantic-to-CNF correctness or Python encoder correctness has
been proved.
